import React from 'react';
import ProjectSelect from 'components/ProjectSelect';

const Header = () => {   
    return(
        <header>
            <ProjectSelect />
        </header>
    );
}

export default Header;
